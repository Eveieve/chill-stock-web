show databases ;
use chillstockDb;
create table user_tbl(
                         username varchar(200),
                         password varchar(200),
                         grantUser varchar(200)
);